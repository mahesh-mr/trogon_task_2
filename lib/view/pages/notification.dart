import 'package:flutter/material.dart';

class Notificaton extends StatelessWidget {
  const Notificaton({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Notificaton'),);
  }
}